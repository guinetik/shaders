{
  "name": "shaders",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/optimize-images.js && vue-tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist",
    "new": "node scripts/new-shader.js",
    "lint:shaders": "node scripts/lint-shaders.js",
    "images": "node scripts/optimize-images.js"
  },
  "dependencies": {
    "shiki": "^3.22.0",
    "vue": "^3.5.13",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@vitejs/plugin-vue": "^5.2.3",
    "gh-pages": "^6.2.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vue-tsc": "^2.2.0"
  }
}
